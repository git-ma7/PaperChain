Steps to run the project:
1) Clone the project
2) create a venv for python
3) run pip install -r requirements.txt
4) cd to frontend and run npm install
5) start the frontend server using npm run dev
6) cd to backend and start the backend server using python manage.py runserver
