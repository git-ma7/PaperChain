Steps to run the project:
1) Clone the project
2) create a venv for python
3) run pip install -r requirements.txt
4) cd to frontend and run npm install
5) cd to smart contracts and run npm install
6) start the frontend server using npm run dev
7) cd to backend and start the backend server using python manage.py runserver
